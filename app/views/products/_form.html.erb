<div class="row">
  <div class="col-sm-6">
    <div class ="errors">

    <%= form_for [@product.store, @product], html: {role: "form"} do |f| %>
      <% if @product.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

          <ul>
          <% @product.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

    <div class="actions">

      <div class="form-group">
      <%=collection_select(:product, :category_id, Category.all, :id, :category_name, prompt: 'Please select a category') %>
      </p>

      <div class="form-group">
      <%= f.label :product_name %></br>
      <%= f.text_field :product_name, id:"product_name", class: 'form-control' %>
      <%= @product.errors[:product_name].join(", ") %>
      </div>

      <div class="form-group">
      <%= f.label :product_description %></br>
      <%= f.text_area :product_description, rows: 8, id:"product_description", class: 'form-control' %>
      <%= @product.errors[:product_description].join(", ") %>
      </div>

      <div class="form-group">
      <%= f.label :product_price %></br>
      <%= f.text_field :product_price, id:"product_price", class: 'form-control' %>
      <%= @product.errors[:product_price].join(", ") %>
      </div>

      <!-- <div class="form-group">
      <%# f.label :product_image %></br>
      <%# f.file_field :images %>
      <%# f.text_field :product_image, id:"product_image", class: 'form-control' %>
      <%# @product.errors[:product_image].join(", ") %>
      </div> -->

    
      <div>
      <%= f.submit :save, class: 'btn btn-default' %>
      </div>

    </div>
    <% end %>

    

    <script id="template-upload" type="text/x-tmpl">
    <div id="upload_{%=o.unique_id%}" class="upload">
    <div id="upload-image"></div>
    <div id="upload-image"></div>
    <div id="upload-image"></div>
    <div id="upload-image"></div>
      <div class="progress progress-striped active"><div class="bar" style="width: 0%"></div>
    </div>
    </script>






    </div>
  </div>
</div>
